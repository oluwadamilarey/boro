import React from "react";
import styled from "styled-components";

const PageContainer = styled.div`

`

const HomePage = () => {
    return(
        <PageContainer>
            Hello
        </PageContainer>
    )
}